<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/bg_white"
    tools:context="com.hanks.healthspy.health.CheckUploadActivity">

    <TextView
        android:id="@+id/heart_rate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="您的心率是"
        android:textSize="30dp"
        android:layout_marginBottom="10dp"
        android:textColor="@color/text_yellow"/>

    <Button
        android:id="@+id/heart_rate_upload_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="上  传"
        android:textSize="20dp"
        android:background="@drawable/btn_shape_sign_in"
        android:layout_below="@+id/heart_rate"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:onClick="heart_rate_upload"/>

    <Button
        android:id="@+id/check_up_cancel_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="取  消"
        android:textSize="20dp"
        android:background="@drawable/btn_shape_sign_out"
        android:layout_alignTop="@+id/heart_rate_upload_btn"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:onClick="cancle_upload"/>

</RelativeLayout>
